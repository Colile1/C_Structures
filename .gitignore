# ── Build output ─────────────────────────────────────────────────
build/
build_win/
build_linux/
build_mac/
build_tests/
build-wasm/
out/

# ── CMake generated (in-source, legacy) ──────────────────────────
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
CTestTestfile.cmake
DartConfiguration.tcl
Makefile
*.cmake
!CMakeLists.txt

# ── Compiled binaries and libraries ──────────────────────────────
*.exe
*.dll
*.so
*.dylib
*.a
*.lib
*.o
*.obj
*.d
*.pdb

# Named binaries (if placed in repo root by accident).
# Anchored + .exe so these don't swallow the C_Structures/ tree or tests/ sources.
/C_Structures
/C_Structures.exe
/tests.exe

# Bundled runtime assets copied into build dirs (originals live in resources/)
build_win/fa-solid-900.ttf
build_win/*.dll

# ── Runtime temporaries ───────────────────────────────────────────
imgui.ini
*.tmp
test_*_tmp.csv
integration_*_tmp.csv

# ── IDE and editor ────────────────────────────────────────────────
.vscode/launch.json
.vscode/c_cpp_properties.json
.idea/
.vs/
*.swp
*.swo
*.suo
*.user
*.ncb
*.sdf

# ── OS artefacts ──────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
ehthumbs.db

# ── Emscripten SDK (installed locally, not committed) ────────────
emsdk/

# ── Internal / meta docs (kept local, not published) ─────────────
.claude/
.vscode/
CLAUDE.md
rules.md
plan.md
todo.md
log.md